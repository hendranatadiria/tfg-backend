<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Bootstrap demo</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-KK94CHFLLe+nY2dmCWGMq91rCGa5gtU4mk92HdvYe+M/SXH301p5ILy+dN9+nJOZ" crossorigin="anonymous">
  </head>
  <body>
    <div class="container container-fluid pt-5">
        <h1>Cloud-Connected Smart Hand Sanitizer Dispenser Data</h1>
        <p>Here is the data from the cloud-connected smart hand sanitizer dispenser.</p>
        <div class="row">
            <div class="col-lg-6 col-sm-12">
                <h2>Temperature</h2>
                <table class="table">
                    <thead>
                        <tr>
                            <td>Device ID</td>
                            <td>Time</td>
                            <td>Temperature</td>
                        </tr>
                    </thead>
                    <% for (let i = 0; i < dataTemp.length; i++) { %>
                        <tr>
                            <td><%= dataTemp[i].device.name %></td>
                            <td><%= dataTemp[i].timestamp %></td>
                            <td><%= dataTemp[i].value %></td>
                        </tr>
                    <% } %>

                </table>
                
            </div>
            <div class="col-lg-6 col-sm-12">
                <h2>Liquid Level</h2>
                <table class="table">
                    <thead>
                        <tr>
                            <td>Device ID</td>
                            <td>Time</td>
                            <td>Distance</td>
                            <td>Percentage</td>
                        </tr>
                    </thead>
                    <% for (let i = 0; i < dataLevel.length; i++) { %>
                        <tr>
                            <td><%= dataLevel[i].device.name %></td>
                            <td><%= dataLevel[i].timestamp %></td>
                            <td><%= dataLevel[i].height %> cm</td>
                            <td><%= dataLevel[i].level %> %</td>
                        </tr>
                    <% } %>

                </table>
            </div>
        </div>

    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ENjdO4Dr2bkBIFxQpeoTz1HIcje39Wm4jDKdf19U8gI4ddQ3GYNS7NTKfAdVQSZe" crossorigin="anonymous"></script>
  </body>
</html>